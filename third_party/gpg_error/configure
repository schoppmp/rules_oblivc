#!/bin/bash

# Copy headers to temporary directory so the configure script can edit them.
SRC_DIR=$EXT_BUILD_ROOT/external/org_gnupg_gpg_error/
cp -Lr $SRC_DIR/* .

# Run autoreconf after applying the patch for gawk 5
autoreconf -vfi

./configure "$@"